program teste (xablau, xablau, xablau);
label
	123, 321, 24;
const
	asd = 321.98;
	dsa = 56;
type
	fdsg = integer;
	gfd = ^real;
	nome = packed array [integer, real] of string;
	asd = file of string;
var
	kjdhb: integer;
	asd, vpoe, djlkh, oh: real;
	rec: record 
		a,b: double;
		case c: integer of
		    'teste': (d, e: string);
		     5.68: (f, g: integer)
	     end;
  arr: array[0..100] of double;

function proc (asd, asd: integer): boolean;
begin
  for i := 0 downto c do
  begin
  end;
end;

procedure proc2(var teste: integer; procedure novoteste(x: double));
begin
	while a < b do 
	begin
		xabale(asd, 123);
		xablau;
	end;
end;

begin

123:
  a := b * (2 mod 5);
  b := proc(1.40, 2);
  proc2;

  if a > 5.40e5 then
  begin
    repeat
      a := a + b;
    until false;
  end
  else
  begin
    while proc(84, 'teste') > 5 do
      xablu('teste');
  end;

  case t of
    5: with teste do
       begin
         goto 5;
       end
  end;
  for ab:=0 to 5 do
	  guizao := 24;

  h := (not b) and c;

  teste := 'reasd';
end.

